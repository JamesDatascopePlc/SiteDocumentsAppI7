import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SiteDocumentsAppI7',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
