import { DatetimePickerComponent } from './datetime-picker.component';

describe('DatetimePicker', () => {
  it('should create an instance', () => {
    expect(new DatetimePickerComponent()).toBeTruthy();
  });
});
