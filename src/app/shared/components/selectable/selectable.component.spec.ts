import { SelectableComponent } from './selectable.component';

describe('SelectableComponent', () => {
  it('should create an instance', () => {
    expect(new SelectableComponent()).toBeTruthy();
  });
});
