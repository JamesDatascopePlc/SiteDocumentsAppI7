import { SignaturePadComponent } from './signature-pad.component';

describe('SignaturePadComponent', () => {
  it('should create an instance', () => {
    expect(new SignaturePadComponent()).toBeTruthy();
  });
});
