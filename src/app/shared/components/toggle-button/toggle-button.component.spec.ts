import { ToggleButtonComponent } from './toggle-button.component';

describe('ToggleButtonComponent', () => {
  it('should create an instance', () => {
    expect(new ToggleButtonComponent()).toBeTruthy();
  });
});
