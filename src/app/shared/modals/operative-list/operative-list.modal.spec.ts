import { OperativeListModal } from './operative-list.modal';

describe('OperativeListModal', () => {
  it('should create an instance', () => {
    expect(new OperativeListModal()).toBeTruthy();
  });
});
