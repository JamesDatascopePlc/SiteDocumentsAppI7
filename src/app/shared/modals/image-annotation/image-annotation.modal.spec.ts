import { ImageAnnotationModal } from './image-annotation.modal';

describe('ImageAnnotationModal', () => {
  it('should create an instance', () => {
    expect(new ImageAnnotationModal()).toBeTruthy();
  });
});
