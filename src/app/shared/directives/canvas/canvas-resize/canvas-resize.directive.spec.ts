import { CanvasResizeDirective } from './canvas-resize.directive';

describe('CanvasResize', () => {
  it('should create an instance', () => {
    expect(new CanvasResizeDirective()).toBeTruthy();
  });
});
