import { CanvasImageDirective } from './canvas-image.directive';

describe('CanvasImageDirective', () => {
  it('should create an instance', () => {
    expect(new CanvasImageDirective()).toBeTruthy();
  });
});
