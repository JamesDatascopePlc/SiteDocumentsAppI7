import { UploadPhotoDirective } from './upload-photo.directive';

describe('UploadPhotoDirective', () => {
  it('should create an instance', () => {
    expect(new UploadPhotoDirective()).toBeTruthy();
  });
});
