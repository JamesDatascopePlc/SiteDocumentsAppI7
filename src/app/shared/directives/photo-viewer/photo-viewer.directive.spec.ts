import { PhotoViewerDirective } from './photo-viewer.directive';

describe('PhotoViewerDirective', () => {
  it('should create an instance', () => {
    expect(new PhotoViewerDirective()).toBeTruthy();
  });
});
