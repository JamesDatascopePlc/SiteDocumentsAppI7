import { CheckboxTextboxComponent } from './checkbox-textbox.component';

describe('CheckboxTextboxComponent', () => {
  it('should create an instance', () => {
    expect(new CheckboxTextboxComponent()).toBeTruthy();
  });
});
