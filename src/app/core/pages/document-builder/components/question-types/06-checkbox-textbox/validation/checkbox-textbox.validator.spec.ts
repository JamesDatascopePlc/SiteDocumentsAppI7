import { CheckboxTextboxValidator } from './checkbox-textbox.validator';

describe('CheckboxTextboxValidator', () => {
  it('should create an instance', () => {
    expect(new CheckboxTextboxValidator()).toBeTruthy();
  });
});
