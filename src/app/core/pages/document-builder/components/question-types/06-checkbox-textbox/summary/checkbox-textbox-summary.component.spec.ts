import { CheckboxTextboxSummaryComponent } from './checkbox-textbox-summary.component';

describe('CheckboxTextboxSummaryComponent', () => {
  it('should create an instance', () => {
    expect(new CheckboxTextboxSummaryComponent()).toBeTruthy();
  });
});
