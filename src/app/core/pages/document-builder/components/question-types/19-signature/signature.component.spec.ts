import { SignatureComponent } from './signature.component';

describe('SignatureComponent', () => {
  it('should create an instance', () => {
    expect(new SignatureComponent()).toBeTruthy();
  });
});
