import { SignatureValidator } from './signature.validator';

describe('SignatureValidator', () => {
  it('should create an instance', () => {
    expect(new SignatureValidator()).toBeTruthy();
  });
});
