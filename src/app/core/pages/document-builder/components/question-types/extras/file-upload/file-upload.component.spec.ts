import { FileUploadComponent } from './file-upload.component';

describe('FileUploadComponent', () => {
  it('should create an instance', () => {
    expect(new FileUploadComponent()).toBeTruthy();
  });
});
