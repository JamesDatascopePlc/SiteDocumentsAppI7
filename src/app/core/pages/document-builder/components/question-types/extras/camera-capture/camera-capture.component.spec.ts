import { CameraCaptureComponent } from './camera-capture.component';

describe('CameraCaptureComponent', () => {
  it('should create an instance', () => {
    expect(new CameraCaptureComponent()).toBeTruthy();
  });
});
