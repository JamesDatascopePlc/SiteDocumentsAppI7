import { NumberSummaryComponent } from './number-summary.component';

describe('NumberSummaryComponent', () => {
  it('should create an instance', () => {
    expect(new NumberSummaryComponent()).toBeTruthy();
  });
});
