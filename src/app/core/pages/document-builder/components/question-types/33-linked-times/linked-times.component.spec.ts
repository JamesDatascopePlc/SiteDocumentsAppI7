import { LinkedTimesComponent } from './linked-times.component';

describe('LinkedTimesComponent', () => {
  it('should create an instance', () => {
    expect(new LinkedTimesComponent()).toBeTruthy();
  });
});
