import { RadioTableTextboxComponent } from './radio-table-textbox.component';

describe('RadioTableTextboxComponent', () => {
  it('should create an instance', () => {
    expect(new RadioTableTextboxComponent()).toBeTruthy();
  });
});
