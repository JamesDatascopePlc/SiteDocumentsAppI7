import { SelectSummaryComponent } from './select-summary.component';

describe('SelectSummaryComponent', () => {
  it('should create an instance', () => {
    expect(new SelectSummaryComponent()).toBeTruthy();
  });
});
