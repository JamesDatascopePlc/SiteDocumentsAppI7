import { AssetListSummaryComponent } from './asset-list-summary.component';

describe('AssetListSummaryComponent', () => {
  it('should create an instance', () => {
    expect(new AssetListSummaryComponent()).toBeTruthy();
  });
});
