import { AssetListValidator } from './asset-list.validator';

describe('AssetListValidator', () => {
  it('should create an instance', () => {
    expect(new AssetListValidator()).toBeTruthy();
  });
});
