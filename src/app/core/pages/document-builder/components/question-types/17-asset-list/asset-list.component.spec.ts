import { AssetListComponent } from './asset-list.component';

describe('AssetListComponent', () => {
  it('should create an instance', () => {
    expect(new AssetListComponent()).toBeTruthy();
  });
});
