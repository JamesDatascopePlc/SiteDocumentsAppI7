import { SelectTextValidator } from './select-text.validator';

describe('SelectTextValidator', () => {
  it('should create an instance', () => {
    expect(new SelectTextValidator()).toBeTruthy();
  });
});
