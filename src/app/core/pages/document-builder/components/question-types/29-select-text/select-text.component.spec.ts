import { SelectTextComponent } from './select-text.component';

describe('SelectTextComponent', () => {
  it('should create an instance', () => {
    expect(new SelectTextComponent()).toBeTruthy();
  });
});
