import { TextboxSummaryComponent } from './textbox-summary.component';

describe('TextboxSummaryComponent', () => {
  it('should create an instance', () => {
    expect(new TextboxSummaryComponent()).toBeTruthy();
  });
});
