import { LinkedTimeComponent } from './linked-time.component';

describe('LinkedTimeComponent', () => {
  it('should create an instance', () => {
    expect(new LinkedTimeComponent()).toBeTruthy();
  });
});
