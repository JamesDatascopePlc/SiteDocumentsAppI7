import { RadioTableComponent } from './radio-table.component';

describe('RadioTableComponent', () => {
  it('should create an instance', () => {
    expect(new RadioTableComponent()).toBeTruthy();
  });
});
