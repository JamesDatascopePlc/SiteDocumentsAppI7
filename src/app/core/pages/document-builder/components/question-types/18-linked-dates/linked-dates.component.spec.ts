import { LinkedDatesComponent } from './linked-dates.component';

describe('LinkedDatesComponent', () => {
  it('should create an instance', () => {
    expect(new LinkedDatesComponent()).toBeTruthy();
  });
});
