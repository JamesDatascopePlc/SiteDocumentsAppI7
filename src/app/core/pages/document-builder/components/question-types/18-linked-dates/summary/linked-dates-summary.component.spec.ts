import { LinkedDatesSummaryComponent } from './linked-dates-summary.component';

describe('LinkedDatesSummaryComponent', () => {
  it('should create an instance', () => {
    expect(new LinkedDatesSummaryComponent()).toBeTruthy();
  });
});
