import { TimeSummaryComponent } from './time-summary.component';

describe('TimeSummaryComponent', () => {
  it('should create an instance', () => {
    expect(new TimeSummaryComponent()).toBeTruthy();
  });
});
