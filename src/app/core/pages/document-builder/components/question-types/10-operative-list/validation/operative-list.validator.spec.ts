import { OperativeListValidator } from './operative-list.validator';

describe('OperativeListValidator', () => {
  it('should create an instance', () => {
    expect(new OperativeListValidator()).toBeTruthy();
  });
});
