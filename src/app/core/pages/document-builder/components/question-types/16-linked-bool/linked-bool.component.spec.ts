import { LinkedBoolComponent } from './linked-bool.component';

describe('LinkedBoolComponent', () => {
  it('should create an instance', () => {
    expect(new LinkedBoolComponent()).toBeTruthy();
  });
});
