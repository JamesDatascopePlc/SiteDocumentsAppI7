import { RadioGroupComponent } from './radio-group.component';

describe('RadioGroupComponent', () => {
  it('should create an instance', () => {
    expect(new RadioGroupComponent()).toBeTruthy();
  });
});
