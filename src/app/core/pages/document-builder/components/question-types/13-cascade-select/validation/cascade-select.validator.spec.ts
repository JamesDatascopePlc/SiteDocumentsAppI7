import { CascadeSelectValidator } from './cascade-select.validator';

describe('CascadeSelectValidator', () => {
  it('should create an instance', () => {
    expect(new CascadeSelectValidator()).toBeTruthy();
  });
});
