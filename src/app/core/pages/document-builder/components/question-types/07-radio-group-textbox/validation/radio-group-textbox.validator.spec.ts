import { RadioGroupTextboxValidator } from './radio-group-textbox.validator';

describe('RadioGroupTextboxValidator', () => {
  it('should create an instance', () => {
    expect(new RadioGroupTextboxValidator()).toBeTruthy();
  });
});
