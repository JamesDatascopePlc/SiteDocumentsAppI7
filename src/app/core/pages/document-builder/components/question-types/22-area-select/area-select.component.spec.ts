import { AreaSelectComponent } from './area-select.component';

describe('AreaSelectComponent', () => {
  it('should create an instance', () => {
    expect(new AreaSelectComponent()).toBeTruthy();
  });
});
