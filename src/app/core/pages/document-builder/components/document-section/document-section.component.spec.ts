import { DocumentSectionComponent } from './document-section.component';

describe('DocumentSectionComponent', () => {
  it('should create an instance', () => {
    expect(new DocumentSectionComponent()).toBeTruthy();
  });
});
