import { MultiCheckboxSectionComponent } from './multi-checkbox-section.component';

describe('MultiCheckboxSectionComponent', () => {
  it('should create an instance', () => {
    expect(new MultiCheckboxSectionComponent()).toBeTruthy();
  });
});
