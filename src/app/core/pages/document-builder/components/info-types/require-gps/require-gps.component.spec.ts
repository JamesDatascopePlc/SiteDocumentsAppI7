import { RequireGpsComponent } from './require-gps.component';

describe('RequireGpsComponent', () => {
  it('should create an instance', () => {
    expect(new RequireGpsComponent()).toBeTruthy();
  });
});
