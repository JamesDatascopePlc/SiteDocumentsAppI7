import { DocumentNavigationComponent } from './document-navigation.component';

describe('DocumentNavigationComponent', () => {
  it('should create an instance', () => {
    expect(new DocumentNavigationComponent()).toBeTruthy();
  });
});
