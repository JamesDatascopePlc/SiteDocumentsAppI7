import { CategoryActionerSelectComponent } from './category-actioner-select.component';

describe('CategoryActionerSelectComponent', () => {
  it('should create an instance', () => {
    expect(new CategoryActionerSelectComponent()).toBeTruthy();
  });
});
