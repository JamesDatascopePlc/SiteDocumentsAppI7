import { QueueSelectComponent } from './queue-select.component';

describe('QueueSelectComponent', () => {
  it('should create an instance', () => {
    expect(new QueueSelectComponent()).toBeTruthy();
  });
});
