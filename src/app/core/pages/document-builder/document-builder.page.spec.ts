import { DocumentBuilderPage } from './document-builder.page';

describe('DocumentBuilder', () => {
  it('should create an instance', () => {
    expect(new DocumentBuilderPage()).toBeTruthy();
  });
});
