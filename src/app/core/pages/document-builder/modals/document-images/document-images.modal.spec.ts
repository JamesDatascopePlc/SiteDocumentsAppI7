import { DocumentImagesModal } from './document-images.modal';

describe('DocumentImagesModal', () => {
  it('should create an instance', () => {
    expect(new DocumentImagesModal()).toBeTruthy();
  });
});
