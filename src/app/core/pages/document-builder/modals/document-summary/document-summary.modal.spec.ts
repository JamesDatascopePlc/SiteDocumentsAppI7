import { DocumentSummaryModal } from './document-summary.modal';

describe('DocumentSummaryModal', () => {
  it('should create an instance', () => {
    expect(new DocumentSummaryModal()).toBeTruthy();
  });
});
