import { MultiCheckboxModal } from './multi-checkbox.modal';

describe('MultiCheckboxModal', () => {
  it('should create an instance', () => {
    expect(new MultiCheckboxModal()).toBeTruthy();
  });
});
