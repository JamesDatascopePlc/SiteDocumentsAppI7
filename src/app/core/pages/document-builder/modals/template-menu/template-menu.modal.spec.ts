import { TemplateMenuModal } from './template-menu.modal';

describe('TemplateMenuModal', () => {
  it('should create an instance', () => {
    expect(new TemplateMenuModal()).toBeTruthy();
  });
});
