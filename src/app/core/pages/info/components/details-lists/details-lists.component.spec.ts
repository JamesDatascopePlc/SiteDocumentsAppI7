import { DetailsListsComponent } from './details-lists.component';

describe('DetailsComponent', () => {
  it('should create an instance', () => {
    expect(new DetailsListsComponent()).toBeTruthy();
  });
});
