import { SiteDocumentImageStore } from './site-document-image.store';

describe('SiteDocumentImageStore', () => {
  it('should create an instance', () => {
    expect(new SiteDocumentImageStore()).toBeTruthy();
  });
});
