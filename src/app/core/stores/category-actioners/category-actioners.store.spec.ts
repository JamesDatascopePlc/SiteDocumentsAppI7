import { CategoryActionersStore } from './category-actioners.store';

describe('CategoryActionersStore', () => {
  it('should create an instance', () => {
    expect(new CategoryActionersStore()).toBeTruthy();
  });
});
