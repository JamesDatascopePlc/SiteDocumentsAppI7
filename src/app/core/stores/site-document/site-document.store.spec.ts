import { SiteDocumentStore } from './site-document.store';

describe('SiteDocumentStore', () => {
  it('should create an instance', () => {
    expect(new SiteDocumentStore()).toBeTruthy();
  });
});
